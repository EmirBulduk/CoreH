chunks:
  buffer-zone: 3  # Minimum distance from other towns
  claim-cost-per-chunk: 50.0
  wilderness-permissions:
    build: true
    destroy: true
war:
  declaration-cost: 10000.0
  preparation-time-minutes: 20